; better-auth declares optional framework peers (sveltekit, next, etc.) that
; we don't use; their transitive vite peers conflict with vitest's vite.
; legacy-peer-deps lets npm install/ci resolve without those false conflicts.
legacy-peer-deps=true
